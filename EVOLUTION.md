# crew 가 달라진 기록

crew 를 계속 최적화하기 위한 기록이다. 세대(과제 한 판을 돌린 단위)마다 한 절씩 늘어난다.

기록은 세 층으로 나눈다. 각 층이 잘하는 것만 맡는다.

| 층 | 무엇 | 어디 |
|---|---|---|
| 무엇을 바꿨나 | 실제 diff 와 이유 | git 커밋 (커밋 글에 근거를 적는다) |
| 왜 바꿨고 먹혔나 | 관찰 → 진단 → 변경 → 예측 → 검증 | 이 파일 |
| 숫자 | 턴·토큰·승인·판 수 | `metrics/runs.jsonl` (`scripts/retro-cost.js --record` 가 붙인다) |

**한 세대가 끝나면 이 파일에 절을 하나 더한다.** 절의 모양은 아래와 같이 고정한다 — 모양이 같아야 세대끼리 견줄 수 있다.

```
## S<번호> — <날짜> · <방> 회차 <N>
돌린 것 / 잰 것(표) / 드러난 것 / 바꾼 것(커밋) / 예측 / 검증
```

**예측 칸이 이 기록의 핵심이다.** 무엇이 좋아질지 미리 적지 않으면 다음 세대에서 효과를 잴 수 없고, 기록은 일기가 된다. 예측은 틀려도 된다 — 틀렸다고 적는 것이 값이다.

---

## S0 — 2026-08-27 ~ 2026-09-08 · 실행 전 (설계만)

**돌린 것**: 없다. 봇을 띄운 적이 없다.

**만든 것**: 커밋 14개 (`fb26e23` ~ `825d911`). 공통 지침·worker 지침·봇별 지침 다섯, `open-room` 스킬, `session-start.js` 훅, `setup.js`, README.

**이때 세운 원칙 셋** (지금도 유효하다):

- 일을 만드는 것은 orchestrator 뿐이다
- 말은 짧게, 내용은 파일에
- 파일이 진실이고 봇의 대화 문맥은 캐시다

**잰 것**: 없다. S1 까지는 가정만 있었고 숫자가 하나도 없었다.

---

## S1 — 2026-09-08 · 수율개선-2026q3 회차 1~2

**돌린 것**: 로트별 수율 CSV(672행) 하나로 "3라인 수율이 왜 떨어졌나"를 묻고, 봇 다섯이 작업 9건을 돌려 의뢰인용 보고서 한 편을 냈다. 결론은 맞았다 — CH-3B 한 챔버가 2026-07-29 에 하루 만에 3.61%p 떨어졌고 늘어난 불량은 전부 파티클.

**잰 것**

| 지표 | 값 |
|---|---|
| 방 전체 | 181분 (08:53~11:55) |
| 그중 세션이 죽어 있던 시간 | 64분 — 사람이 세 번 찔러서 발견 |
| 봇 활성 구간 | 97분 |
| API 호출 | 612회 (작업 9건 → 작업당 68회) |
| 문맥 읽기 / 출력 | 69.8M / 714K (표준단가 환산 약 $62~67) |
| 호출당 평균 문맥 | 114K · 압축은 0회 |
| 사람이 누른 도구 승인 | 78회 (Bash 76 · Edit 2, 절대 경로 포함 73) |
| 진전 없는 왕복 | 3구간 |
| 판 겹침 | 3겹 (task-2 → task-5 → task-8) |
| 봇이 추가로 띄운 서브에이전트 | 11개 |

**드러난 것**

- **답은 10:33 에 이미 나와 있었다.** 보고서를 건넨 것은 11:45. 그 사이 72분에 새로 밝혀진 사실은 없고 전부 기록 정리·판 관리·자기 문서 고치기였다. 작업 9건 중 답을 만든 것은 2건(분석·보고서), 검증 1건, 나머지 6건은 정리와 재작업.
- **문맥이 한 번도 안 비워졌다.** 압축 문턱이 70만인데 실제 최고 문맥은 22만이라 발동 자체를 안 했다. "대화 문맥은 캐시다"라는 원칙에 그 캐시를 비우는 장치가 없었다.
- **승인 78회는 규칙끼리 충돌한 결과다.** allow 는 명령 이름으로 맞는데, 공통 지침이 스스로 `git commit … && git push` 라는 복합 형태를 권하고 있었다.
- **되돌이가 모양을 바꿔 재발했다.** 무멘션 되돌이는 안 났지만 "확인 답장" 왕복이 세 번 났고, 매번 orchestrator 가 손으로 끊었다.
- **멘션 카운터가 회차 2 내내 2 에 멈춰 있었다.** 실제는 21. 되돌이를 잡으라고 둔 장치가 장식이 됐다.
- **내부 은어가 의뢰인에게 그대로 나갔다.** 원장 표의 행을 사람에게 붙여넣고, 보고서 본문에 `status: void` 와 판 이력이 새어 나왔다.
- **보고 서식 200자가 압축을 강제했다.** 조사·어미가 지워진 전보문체가 나왔고, 그런데도 200자 초과가 18건이었다.
- **tick 이 없다.** `minidiscord` 서버에 봇을 주기적으로 깨우는 코드가 없다(전수 검색 0건). 64분 공백의 원인이고 규칙으로는 못 고친다.
- **잘 된 것**: 남의 폴더에 쓴 커밋 0건(51개 전수 확인), supersedes/void 사슬 정확, 교차 검토가 실제 결함을 찾음(검색 범위 누락·지표 정의 혼용·통계 설명 오류·인과 단정).

근거: `.moai/reports/` 의 S1 분석, 방 대화 214건, 봇 세션 기록 5개.

**바꾼 것** — `abe64a6` 한 커밋에 열 가지

| # | 무엇 | 어디 |
|---|---|---|
| 1 | 압축 문턱 70만 → 65만 (창의 65%) | `settings.template.json` |
| 2 | 명령은 하나씩·이름으로 · allow 확장 | `CLAUDE-common.md` · `settings.template.json` |
| 3 | 사건 처리의 state 저장 2회 → 1회 | `orchestrator/CLAUDE.md` |
| 4 | 통과 전 산출물은 같은 파일을 고친다 | `CLAUDE-common.md` · `orchestrator/CLAUDE.md` |
| 5 | "사람에게 하는 말" 절 신설 | `CLAUDE-common.md` |
| 6 | 보고 서식 200자 → 400자 · 말투 균형추 | `CLAUDE-worker.md` |
| 7 | "의뢰인이 읽을 보고서" 절 신설 | `reporter/CLAUDE.md` |
| 8 | 멘션 재계산 · 확인 답장 금지 | `orchestrator/CLAUDE.md` · `CLAUDE-worker.md` |
| 9 | 상태줄을 봇 설정에 | `settings.template.json` · `setup.js` |
| 10 | 회고 장치 | `orchestrator/CLAUDE.md` · `scripts/retro.js` · `scripts/retro-cost.js` |

함께: 결재된 제안 001 반영(`46d65c0`), 제안 002 우회 규칙.

**예측**

| 지표 | S1 | 다음 세대 목표 |
|---|---|---|
| 도구 승인 | 78회 | 10회 이하 |
| 진전 없는 왕복 | 3구간 | 0 |
| 판 겹침 | 3겹 | 1겹 |
| 멘션 카운터 | 기록 2 / 실제 21 | 일치 |
| 작업당 API 호출 | 68회 | 40회 이하 |
| 회고 파일 | 없음 | 생성됨 |

**검증**: S2 절에서.

---

## S1.5 — 2026-09-08 밤 · 상태줄 (같은 것을 세 번 고쳤다)

세대 사이에 낀 작은 판이지만, **"고쳤다고 생각했는데 아니었다"가 세 번 이어진 기록**이라 남긴다.

| 시도 | 무엇을 했나 | 왜 틀렸나 | 커밋 |
|---|---|---|---|
| 1 | 사람 개인 스크립트를 봇 설정에 절대 경로로 | 그 스크립트는 모든 칸을 stdin 에서 읽어 값이 비면 엉뚱한 기본값을 보인다 | `b79c56f` 이전 |
| 2 | crew 자체 스크립트로 바꾸고 훅처럼 상대 경로 | **상태줄은 상대 경로를 주면 Claude Code 가 아예 부르지 않는다.** 훅은 되는데 상태줄은 안 된다 | `b79c56f` → `17cd684` |
| 3 | 절대 경로로 되돌림 | 불리기는 하는데 로봇 이모지 하나만 나왔다 | `17cd684` → `551181b` |
| 4 | PATH 에 기대지 않게 | **Claude Code 는 상태줄 명령을 PATH 가 빈 환경에서 부른다.** `basename`·`jq`·`git` 이 전부 실패해 모든 칸이 비었다 | `551181b` |

**배운 것 둘**

- **추측으로 원인을 말하지 않는다.** 1차 진단("jq 가 없다")은 틀렸다. macOS 에는 `/usr/bin/jq` 가 기본으로 있다. 살아 있는 세션의 스크립트에 심장박동을 심어 실제로 재고 나서야 원인이 잡혔다. 상태줄 스크립트는 매번 새로 읽히므로 재시작 없이 잴 수 있다.
- **훅에서 되는 것이 상태줄에서 된다는 보장이 없다.** 같은 설정 파일 안에 있어도 실행 환경이 다르다.

이 PATH 문제는 상태줄만의 것이 아니었다 — S2 에서 봇의 Bash 도구에서도 같은 증상이 나왔다. 아래를 보라.

---

## S2 — 2026-09-09 · 수율개선-2026q3 회차 3 (진행 중)

**돌린 것**: 설비 이력 CSV(48행)를 주고 "07-29 에 CH-3B 에 무엇이 있었나"를 데이터로 말할 수 있는 데까지 밝히라고 했다. 데이터에 답(같은 날 CH-3A·CH-3B 둘 다 정기정비, CH-3B 만 샤워헤드 교체, 떨어진 것은 CH-3B 뿐)과 함정 넷(중복 행·날짜 형식·표기 흔들림·빈 칸)을 심었다.

**여기까지 드러난 것**

- **회고 장치가 돌았다.** 회차 2 가 닫히면서 `retro-2.md` 가 실제로 생겼고, `retro.js` 가 멘션 카운터 고장(기록 2 / 실제 21)을 스스로 잡아냈다. 봇이 그 자리에서 제안 006·007 을 냈다.
- **봇이 스스로 낸 제안이 넷 더 늘었다** (006~009). 006 은 멘션 세는 자리를 3단계에서 1단계로, 007 은 검토자가 "문서가 스스로 센 숫자"를 다시 세게, 008 은 사람에게는 `@TO` 를 붙이지 않게, 009 는 아래 PATH 문제.
- **PATH 문제가 봇의 Bash 도구에도 있다.** orchestrator 와 archivist 가 각각 `git`·`ls`·`grep` 이 "command not found" 라고 보고했다. 세션 프로세스의 PATH 에는 `/usr/bin` 이 있는데 도구가 도는 환경에는 없다 — 상태줄에서 본 것과 같은 계열이다. **이 때문에 회차 3 의 기록·회고·제안이 전부 미커밋으로 쌓였다.** 제안 009 가 지침에 빠져나갈 문을 내자고 한다.
- **S1 의 변경 4번이 실제로 먹었다.** 작업 9·10 에서 "뜻이 바뀌지 않는 정정"을 새 판으로 만들지 않고 다음 작업의 끝 조건에 얹었다(decisions 34·40행). S1 이라면 판이 더 쌓였을 자리다.

**중간 계측** (회차 3 진행 중, message 217~232)

| 지표 | S1 | S2 중간 | 판정 |
|---|---|---|---|
| 도구 승인 요청 | 78회 | **1회** | 목표(10회 이하) 달성 |
| 봇 발언 1건당 승인 | 1.44 | **0.11** | 13배 줄었다 |
| 시스템 잡음이 방에서 차지하는 비율 | 71% (152/214) | **19%** (3/16) | 방이 읽을 만해졌다 |
| 뜻이 안 바뀌는 정정 | 새 판을 냄 | 다음 작업 끝 조건에 얹음 | 변경 4번이 먹었다 |
| 회고 파일 | 없음 | `retro-2.md` 생성 | 장치가 돌았다 |

**이 숫자를 그대로 믿으면 안 되는 이유**: 표본이 작고(봇 발언 9건), orchestrator 가 PATH 문제로 커밋을 아예 못 하고 있다. 커밋을 안 하면 승인도 안 뜬다 — 줄어든 승인 중 얼마가 규칙 덕이고 얼마가 막혀서인지는 PATH 를 고친 뒤에 다시 재야 갈린다.

**검증** (회차 3 작업 10~13 완료 후)

| S1 예측 | 결과 | 판정 |
|---|---|---|
| 도구 승인 78회 → 10회 이하 | **1회** | 달성. 다만 아래 단서 |
| 진전 없는 왕복 3구간 → 0 | **0** | 달성 |
| 판 겹침 3겹 → 1겹 | **0겹** | 초과 달성 |
| 멘션 카운터 기록≠실제 → 일치 | retro.js 가 불일치를 잡아냄 | 장치는 달성, 카운터는 여전히 고장 |
| 작업당 API 호출 68 → 40 이하 | 192 | **빗나감** (작업 크기가 달라 이 잣대가 부적절) |
| 회고 파일 생성 | `retro-2.md` | 달성 |

**세대 계측**

| 지표 | S1 | S2 |
|---|---|---|
| API 호출 | 771 | 769 |
| 출력 토큰 | 798,953 | **1,109,174** |
| 문맥 읽기 | 77.6M | **105.7M** |
| 표준단가 환산 | $72.39 | **$98.57** |
| 실제 계산 시간 | 118분 | 106분 |
| 산출물 | 189KB / 2,311줄 | 336KB / 4,371줄 |
| 봇 발언 길이 중앙값 | 630자 | **1,280자** |
| 6줄 규칙 위반율 | 46% | **79%** |
| 최대 문맥 | 226K | **273K** (압축 0회) |
| 클라이언트 보고서 | 142줄 | **263줄** |

**드러난 것 — 좋은 쪽**

- 승인·잡음·재작업·확인답장은 설계대로 잡혔다. 특히 판 겹침 0 은 변경 4번이 실제로 먹은 증거다.
- 실제 계산 시간이 118분 → 106분으로 줄었다. 산출물은 1.8배인데도 줄었다.

**드러난 것 — 나쁜 쪽 (이쪽이 더 중요하다)**

- **잣대를 잘못 골랐다.** "산출물 1KB당 비용 24% 감소"를 성과로 적었는데, 이 지표는 **같은 결론을 더 길게 쓰면 좋아진다.** 생산성 지표가 아니다. 폐기한다.
- **상한을 올린 것이 목표치가 됐다.** 보고 칸을 200자 → 400자로 넓혔더니, S1 에서 0칸이던 초과가 S2 에서 처음 나왔다. 봇 발언은 사람 글(98자)의 12배다.
- **"짧게 쓰되 글자 수가 목표가 아니다"가 상한을 두지 않을 근거로 읽혔다.** 세는 자가 없으니 봇이 자기 검사를 못 한다. 25어절 초과 문장 비율이 6.6% → 10.2% 로 늘었다.
- **검토를 여러 번 하고도 오류가 남았다.** 클라이언트 보고서에 확인된 결함 둘:
  - 분석의 "뒤가 19일까지만"(개수)이 보고서에서 "뒤쪽이 08-19 까지만"(날짜)으로 바뀌었다.
  - 봇이 한 재계산을 "**다른 사람이** … 다시 계산해"라고 썼다. 사람의 독립 검증으로 읽힌다.
- **승인 78 → 1 을 그대로 성과로 읽으면 안 된다.** 같은 회차에 PATH 고장으로 두 봇이 커밋을 0건 했다. 실행이 실패해서 조용해진 것과 자동화가 는 것은 다르다.

**바깥 평가** (다른 모델의 독립 판정): "지금은 싱글 세션을 이기는 시스템이 아니라, 값 있는 교차 검토 한 번에 과도한 관리·기록·재서술 비용을 붙인 시스템이다." 다섯 봇이 산 것으로 인정된 것은 **별도 문맥에서의 독립 재계산 한 번**이고, 그것이 나머지 세 역할의 상주 비용을 정당화하지는 못한다는 판정이다.

---

## S3 계획 — 무엇을 시험할 것인가

S1→S2 에서 배운 가장 큰 것: **대조군 없이 세대를 견주면 무엇이 좋아졌는지 알 수 없다.** S1 과 S2 는 입력도 작업 범위도 달라서 직접 대조군이 아니었다. S3 는 그것부터 만든다.

**첫째 — 싱글 기준선을 만든다 (다른 모든 것보다 먼저)**

같은 원본·같은 요청·같은 모델로 셋을 돌려 견준다: ① 싱글 세션 ② 주 세션 + 검토 세션 ③ 지금의 다섯 봇. 완성된 답을 싱글에 미리 주지 않는다. 판정은 출처를 가린 채 사람이 한다 — 정확성 · 인과 과장 · 다음 행동의 명확함 · 읽는 시간.

**이 기준선이 없으면 나머지 개선은 방향을 모른 채 손대는 것이다.**

**둘째 — 절제를 부탁이 아니라 장치로 만든다**

규칙으로 "짧게 쓰라"는 두 세대 연속 실패했다. 봇에게 보이는 자리에 숫자를 놓는다.
- 보내기 전 **자기 계측**: 실제 보낼 문자열의 글자·줄·어절을 세고 상한과 함께 적는다. "600자라고 생각한다"는 무효다.
- 메시지는 **전체 600자·6줄을 동시에** 건다. 칸마다 400자는 총량 제한이 없어 채우기 쉬운 구조였다.
- 문장은 **20어절, 25어절 넘으면 그 산출물은 아직 안 끝났다**.
- 보고서 본문 **4,000자 · 한계 5항목 · 다음 제안 3항목**. 넘치면 지우지 말고 근거 파일로 옮기고 경로만 남긴다.
- **한계는 예산에서 빼지 않는다.** 분량을 맞추려고 "가를 수 없다"를 지우면 실패다. 반복 근거부터 지운다.

**셋째 — 검토 통과를 실제 제출본에 묶는다**

S2 에서 검토자가 필수 정정 둘을 찾고도 "통과"를 줬고, orchestrator 가 그것을 받지 않고 반려해서 막았다. 검토자의 판정이 약했다.
- **필수 정정이 하나라도 남으면 통과가 아니다.**
- 검토자는 **문서가 스스로 센 숫자와 다른 파일에서 옮겨 온 값**을 그 자리에서 다시 센다. (제안 007 이 같은 것을 짚었다)

**넷째 — 구성을 줄여 본다**

바깥 평가의 권고: 주 세션 하나 + 필요할 때만 켜는 독립 검토 하나. 줄이는 순서는 archivist → orchestrator(주 세션에 합침) → reporter, researcher 를 마지막까지 남긴다. **기준선 실험의 ② 안이 곧 이 구성이다** — 따로 결정하지 않고 실험 결과로 정한다.

**다섯째 — 문맥은 압축 문턱을 바꿔 가며 시험한다**

`CREW_AUTOCOMPACT=<값> node setup.js` 로 문턱이 변수가 됐다. 65만은 두 세대 연속 0회 발동이었다. 12만·20만을 걸어 보고, **압축 뒤 handoff·notes 만으로 이어지는지**부터 확인한다. 압축 횟수를 늘리는 것 자체는 성공이 아니다.

**폐기하는 지표**: 산출물 1KB당 비용·호출. 길게 쓰면 좋아진다.
**새로 볼 지표**: 같은 품질에 드는 비용과 시간(기준선 대비) · 최종 산출물에 남은 오류 수 · 사람이 읽는 시간.

**아직 안 고친 것**

- tick 없음 (서버 쪽 일)
- 제안 002 (서버 멘션 배달) · 003·004·005·008·009 (공통 지침, 사람이 고칠 것)
- `crew/.moai/state/*` 가 저장소에 추적되어 세션마다 동기화를 막는다

---

## 다음 세대를 여는 사람에게

1. 회차가 닫히면 `node scripts/retro.js <방> <회차>` 와 `node scripts/retro-cost.js --since <날짜> --room <방번호> --record`
2. 이 파일에 절을 하나 더한다. 앞 절의 **예측 칸을 검증 칸으로 채우는 것부터** 한다
3. 규칙을 고쳤으면 커밋 글에 무엇을 보고 고쳤는지 적는다 — 숫자든 message id 든
4. 예측이 빗나갔으면 그렇게 적는다. 같은 자리를 두 번 고치기 전에 진단을 다시 한다
